[package]
name = "armada-strike"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.16", features = ["wav", "mp3", "vorbis"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8"
dirs = "5.0"